#include "holberton.h"

/**
 * add - adds to numbers
 * @a: the first number to add
 * @b: the second number to add
 *
 * Return: the addition of the two numbers
 */
int add(int a, int b)
{
	return (a + b);
}
