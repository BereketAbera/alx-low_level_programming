#include <stdio.h>

/**
 * main - print a string to the standard output
 *
 * Return: integer to signify success or failure
 */
int main(void)
{
	printf("ith proper grammar, but the outcome is a piece of art,");
	return (0);
}
